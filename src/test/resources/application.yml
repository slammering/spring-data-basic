spring:
  application:
    name: Spring Basic
  main:
    banner-mode: off
  liquibase:
    change-log: classpath:/db/changelog/db.changelog-main.xml
  datasource:
    url: jdbc:h2:mem:testdb;MODE=MSSQLServer
    username: sa
    password: password
